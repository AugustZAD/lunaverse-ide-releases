# Lunaverse IDE — releases

Downloads + auto-update feeds for Lunaverse IDE.

- `latest.json` — full-app auto-update feed (read by the update worker)
- `manifest.json` — extension hot-patch feed
- Releases `vX.Y.Z` — `.dmg` (first install) + `.zip` (auto-update package)
